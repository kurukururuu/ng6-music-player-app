# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.*.local

# Local ngrok agent config — may contain a personal authtoken, never commit
ngrok.yml

# Local, machine-specific agent configuration — never commit
.claude/settings.local.json
CLAUDE.local.md
.github/copilot/settings.local.json

# Volatile deep-scan working state — deleting it loses nothing curated
.sdd-scan/

# Volatile template-update working state — deleting it loses nothing curated
.sdd-update/
